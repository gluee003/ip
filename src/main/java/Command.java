/**
 * The Command enum lists the possible types of commands supported by the bot.
 */
public enum Command {
    TODO,
    DEADLINE,
    EVENT,
    LIST,
    MARK,
    UNMARK,
    BYE,
    UNKNOWN,
}
